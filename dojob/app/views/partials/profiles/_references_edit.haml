%p.text-muted.instr Click on a field you would like to edit. Changes are saved automatically.
- @references.each_with_index do |reference, index|
  %form{ id: "#{reference.id}" }
    .col-md-12.text-left
      %h4.ref-title
        = link_to reference_path(reference.id), method: :delete, remote: true do
          %span.glyphicon.glyphicon-remove.remove{ title: true, data: { toggle: :tooltip, "original-title" => "Remove this reference. Cannot be undone." } }
        = "Reference ##{index + 1}"
    .col-md-12.ref-row.text-left
      .col-sm-3.first-field
        .form-group
          .col-sm-12
            %label.control-label First Name
            %hr.ref-hr
          .col-sm-12
            = best_in_place reference, :first_name, as: :input, place_holder: "First name", inner_class: "form-control", url: reference_path(reference.id)
      .col-sm-3
        .form-group
          .col-sm-12
            %label.control-label Last Name
            %hr.ref-hr
          .col-sm-12
            = best_in_place reference, :last_name, as: :input, place_holder: "Last name", inner_class: "form-control", url: reference_path(reference.id)
      .col-sm-3
        .form-group
          .col-sm-12
            %label.control-label Email
            %hr.ref-hr
          .col-sm-12
            = best_in_place reference, :contact_email, as: :input, place_holder: "Email", inner_class: "form-control", url: reference_path(reference.id)
      .col-sm-3
        .form-group
          .col-sm-12
            %label.control-label Phone
            %hr.ref-hr
          .col-sm-12
            = best_in_place reference, :contact_number, as: :input, place_holder: "1234567891", inner_class: "form-control", url: reference_path(reference.id)
