- if @my_offers.empty?
  .alert.alert-info.offer-alert You have made no offers.
- else
  %table.table.table-striped#offers-table
    %thead
      %tr
        %td Job Title
        %td Posting Date
    %tbody
      - @my_offers.sort_by { |o| o.created_at }.reverse!.each do |o|
        %tr
          %td
            = format_offer_name(o)
            %br
            %p.offer-name= o.made_by
          %td
            %p
              = o.created_at.strftime("%-m/%d/%Y")
              - if o.accepted
                %span.glyphicon.glyphicon-ok.offer-accepted
