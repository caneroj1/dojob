- if @offers_for_me.empty?
  .alert-info.offer-alert There are no offers for you.
- else
  %table.table.table-striped#offers-table
    %thead
      %tr
        %td Job Title
        %td Posting Date
    %tbody
      - @offers_for_me.each do |o|
        %tr
          %td
            %a= format_offer_name(o)
            %br
            %p.offer-name= o.made_by
          %td
            %p= o.created_at.strftime("%-m/%d/%Y")
